name: Test Cross-Repo Call
on:
  push:
    branches: [test/intent-resolver-e2e]
jobs:
  test:
    uses: opsmax/platform-core/.github/workflows/test-reusable.yml@main
    with:
      message: 'Cross-repo with perms + secrets!'
      dry_run: true
    secrets:
      PLATFORM_APP_PRIVATE_KEY: ${{ secrets.PLATFORM_APP_PRIVATE_KEY }}
